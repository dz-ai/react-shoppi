import {useCallback, useEffect} from "react";

export const useOutClick = () => {
    return useCallback((ref, setClose) => {

            useEffect(() => {

            }, []);

        },
        [],
    );

}